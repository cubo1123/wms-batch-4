import React from 'react'
import ReactDOM from 'react-dom'
import MyComponent from './components/App'

const appContainer = document.getElementById('app-element')

ReactDOM.render(<MyComponent />, appContainer)
